# 🎯 سیستم تحلیل هوشمند تماس‌های فروش

یک سیستم پیشرفته برای تحلیل تماس‌های فروش با استفاده از هوش مصنوعی OpenAI و Flask

![Python](https://img.shields.io/badge/Python-3.8+-blue.svg)
![Flask](https://img.shields.io/badge/Flask-3.0.0-green.svg)
![OpenAI](https://img.shields.io/badge/OpenAI-GPT--4-orange.svg)

## ✨ ویژگی‌ها

- 🤖 **تحلیل هوشمند با GPT-4**: استفاده از قدرتمندترین مدل OpenAI
- 📊 **داشبورد تعاملی**: نمایش گرافیکی و زیبای نتایج
- 🎨 **طراحی مدرن**: رابط کاربری حرفه‌ای با Tailwind CSS
- 📈 **نمودارهای پیشرفته**: نمایش تحلیل‌ها با Chart.js
- 🔒 **امنیت بالا**: استفاده از .env برای محافظت از کلیدها
- 🌐 **زبان فارسی**: پشتیبانی کامل از راست‌چین و فونت‌های فارسی

## 📋 پیش‌نیازها

- Python 3.8 یا بالاتر
- کلید API از OpenAI
- مرورگر مدرن (Chrome, Firefox, Safari)

## 🚀 نصب و راه‌اندازی

### 1. کلون کردن پروژه

```bash
git clone <repository-url>
cd sales-call-analyzer
```

### 2. ایجاد محیط مجازی (Virtual Environment)

```bash
# در ویندوز
python -m venv venv
venv\Scripts\activate

# در لینوکس/مک
python3 -m venv venv
source venv/bin/activate
```

### 3. نصب کتابخانه‌ها

```bash
pip install -r requirements.txt
```

### 4. تنظیم کلید API

1. فایل `.env.example` را کپی کنید:
```bash
cp .env.example .env
```

2. فایل `.env` را باز کنید و کلید API خود را وارد کنید:
```
OPENAI_API_KEY=sk-your-actual-api-key-here
```

**نکته**: برای دریافت کلید API به [OpenAI Platform](https://platform.openai.com/api-keys) مراجعه کنید.

### 5. اجرای برنامه

```bash
python app.py
```

برنامه روی آدرس `http://localhost:5000` اجرا می‌شود.

## 📖 نحوه استفاده

### 1. ورود اطلاعات تماس

- نام فروشنده را وارد کنید
- نام مشتری را وارد کنید (اختیاری)
- نام محصول یا خدمت را مشخص کنید
- متن کامل مکالمه را در کادر متن قرار دهید

### 2. تحلیل با AI

روی دکمه "تحلیل هوشمند با AI" کلیک کنید. سیستم چند ثانیه طول می‌کشد تا تحلیل کامل انجام شود.

### 3. مشاهده نتایج

نتایج شامل موارد زیر است:

- **خلاصه کلی**: یک خلاصه یک‌خطی از تماس
- **امتیازات کلیدی**: امتیازهای برقراری ارتباط، نیازسنجی، بستن فروش
- **نمودارها**: نمودار رادار مهارت‌ها و نمودار DISC
- **تحلیل شخصیت**: تیپ شخصیتی مشتری و راهنمای تعامل
- **نقاط قوت و ضعف**: تحلیل دقیق عملکرد فروشنده
- **اعتراضات**: اعتراضات مطرح شده و نحوه پاسخ
- **تکنیک‌ها**: تکنیک‌های فروش استفاده شده
- **اقدام بعدی**: پیشنهاد گام‌های بعدی

## 🎨 ساختار پروژه

```
sales-call-analyzer/
│
├── app.py                 # فایل اصلی Flask
├── requirements.txt       # کتابخانه‌های Python
├── .env                   # تنظیمات محیطی (کلید API)
├── .env.example           # نمونه فایل تنظیمات
├── README.md              # راهنمای پروژه
│
├── templates/
│   └── index.html         # صفحه اصلی داشبورد
│
└── static/
    └── app.js             # JavaScript برای تعاملات
```

## 🔧 تنظیمات پیشرفته

### تغییر مدل OpenAI

در فایل `app.py` خط زیر را پیدا کنید:

```python
model="gpt-4o-mini"
```

می‌توانید مدل را به یکی از موارد زیر تغییر دهید:
- `gpt-4o-mini`: سریع‌تر و ارزان‌تر
- `gpt-4o`: قدرتمندتر و دقیق‌تر
- `gpt-4-turbo`: متعادل

### تنظیم دما (Temperature)

برای کنترل خلاقیت پاسخ‌های AI:

```python
temperature=0.7  # بین 0 تا 1 (پایین‌تر = محافظه‌کارتر)
```

## 📊 فیلدهای تحلیل

سیستم 50+ فیلد مختلف را تحلیل می‌کند:

### فیلدهای عددی
- امتیازهای مهارتی (0-10)
- پرسونای DISC
- درصدها و آمارها

### فیلدهای متنی
- خلاصه تماس
- نقاط قوت و ضعف
- اعتراضات و تکنیک‌ها
- راهنمای تعامل با مشتری

## 🐛 رفع مشکلات

### خطا: "کلید API تنظیم نشده"

1. مطمئن شوید فایل `.env` وجود دارد
2. بررسی کنید که کلید با `sk-` شروع می‌شود
3. برنامه را دوباره راه‌اندازی کنید

### خطا در نصب کتابخانه‌ها

```bash
# به‌روزرسانی pip
pip install --upgrade pip

# نصب مجدد
pip install -r requirements.txt --no-cache-dir
```

### مشکل در نمایش فونت فارسی

مرورگر خود را refresh کنید (Ctrl+F5) یا cache را پاک کنید.

## 💡 نکات مهم

1. **هزینه API**: هر تحلیل مقداری از کردیت OpenAI شما را مصرف می‌کند
2. **کیفیت ورودی**: هرچه متن مکالمه دقیق‌تر باشد، تحلیل بهتر است
3. **زمان تحلیل**: تحلیل معمولاً 10-30 ثانیه طول می‌کشد
4. **امنیت**: هرگز فایل `.env` را commit نکنید

## 🔐 امنیت

- از `.gitignore` برای محافظت از `.env` استفاده کنید
- کلید API را در محیط‌های عمومی share نکنید
- برای production از متغیرهای محیطی سرور استفاده کنید

## 📝 مثال متن تماس

```
فروشنده: سلام، روز بخیر. احمد رضایی هستم از شرکت تکنولوژی ایده‌آل.
مشتری: سلام، بفرمایید.
فروشنده: با آقای محمدی صحبت می‌کنم؟
مشتری: بله، خودم هستم.
فروشنده: عالی. می‌خواستم در مورد نرم‌افزار CRM جدیدمون باهاتون صحبت کنم...
```

## 🤝 مشارکت

برای مشارکت در پروژه:

1. Fork کنید
2. یک branch جدید بسازید
3. تغییرات خود را commit کنید
4. Pull Request بزنید

## 📄 لایسنس

این پروژه تحت لایسنس MIT منتشر شده است.

## 👨‍💻 توسعه‌دهنده

ساخته شده با ❤️ برای تحلیل بهتر تماس‌های فروش

## 🆘 پشتیبانی

برای گزارش باگ یا درخواست ویژگی جدید، از بخش Issues استفاده کنید.

---

**نکته**: این پروژه از OpenAI API استفاده می‌کند و نیاز به کلید معتبر دارد.